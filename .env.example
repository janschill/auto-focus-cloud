DATABASE_URL=storage/data/autofocus.db

# Slack OAuth Configuration
SLACK_CLIENT_ID=your_slack_client_id_here
SLACK_CLIENT_SECRET=your_slack_client_secret_here
SLACK_REDIRECT_URI=https://auto-focus.app/api/slack/oauth/callback

# Mailgun SMTP Configuration
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=no-reply@auto-focus.app
SMTP_PASS=your-mailgun-smtp-password

SENTRY_DSN=
PORT=8080

# HMAC Secret for license validation response signing
# Generate with: head -c 32 /dev/urandom | base64
HMAC_SECRET=your_random_32_byte_secret_here
